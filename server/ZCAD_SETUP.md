# Настройка ZCAD для работы с GRIST виджетами

## ⚠️ Важно

Файлы `uzvipcintegration.pas` и `uzvipcserver.pas` в папке `widget/` - это **исходный код для ZCAD**, который должен быть:
1. Скомпилирован в составе ZCAD
2. Загружен как часть ZCAD

Если вы видите ошибку `Connection refused - is ZCAD running?`, значит IPC сервер ZCAD не запущен.

## 🔧 Настройка ZCAD

### Вариант 1: IPC сервер уже встроен в ZCAD

1. Откройте ZCAD
2. В командной строке ZCAD введите:
   ```
   IPCStart 127.0.0.1 7777
   ```
3. Проверьте статус:
   ```
   IPCStatus
   ```

### Вариант 2: IPC сервер не встроен (требуется компиляция)

Если команды `IPCStart` не существует, нужно пересобрать ZCAD с поддержкой IPC:

1. Откройте проект ZCAD в Lazarus/Free Pascal
2. Добавьте в проект файлы:
   - `widget/uzvipcserver.pas`
   - `widget/uzvipcintegration.pas`
3. Убедитесь что в главном цикле ZCAD вызывается `IPCProcessPendingCommands`
4. Пересоберите ZCAD

### Вариант 3: Использование готовой версии ZCAD

Если ZCAD поставляется с IPC поддержкой:
1. Убедитесь что ZCAD запущен
2. Проверьте что порт 7777 не занят другой программой
3. Запустите IPC сервер командой `IPCStart`

## 🧪 Проверка работы

### Тест 1: Проверка порта

```bash
netstat -ano | findstr :7777
```

Если порт не отображается - ZCAD IPC сервер не запущен.

### Тест 2: Проверка из Python

```bash
cd c:\zcad\GristWidgets\server
..\python\python.exe zcad_tcp_client.py
```

Ожидаемый результат:
```
ZCAD доступен! Тест создание 10 случайных линий...
Успешно выполнено: 12/12 команд
```

### Тест 3: Проверка из Flask

```bash
curl -X POST http://127.0.0.1:5000/api/zcad/ping
```

Ожидаемый результат:
```json
{"status":"ok","message":"ZCAD connected","timestamp":"..."}
```

## 🔍 Диагностика

### Ошибка: Connection refused

**Причина:** ZCAD не запущен или IPC сервер не активен

**Решение:**
1. Запустите ZCAD
2. Выполните `IPCStart` в ZCAD
3. Проверьте брандмауэр Windows

### Ошибка: Timeout

**Причина:** ZCAD заблокирован или перегружен

**Решение:**
1. Увеличьте таймаут в `zcad_tcp_client.py`
2. Проверьте что ZCAD не в фоновом режиме
3. Перезапустите ZCAD

### Ошибка: Unknown command

**Причина:** Команды не обрабатываются в главном потоке ZCAD

**Решение:**
1. Убедитесь что `uzvipcintegration.pas` загружен в ZCAD
2. Проверьте что `IPCProcessPendingCommands` вызывается в главном цикле

## 📋 Чеклист готовности

- [ ] ZCAD запущен
- [ ] Команда `IPCStart` выполнена успешно
- [ ] Порт 7777 слушается (проверка через `netstat`)
- [ ] `zcad_tcp_client.py` успешно выполняет ping
- [ ] Flask API возвращает `{"status":"ok"}` на `/api/zcad/ping`

## 📚 Дополнительные ресурсы

- Исходный код IPC сервера: `widget/uzvipcserver.pas`
- Интеграция с главным циклом: `widget/uzvipcintegration.pas`
- Python клиент: `server/zcad_tcp_client.py`
