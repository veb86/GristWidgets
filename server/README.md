# Random Line Widget для GRIST

Виджет для управления ZCAD через TCP из GRIST. Позволяет создавать случайные линии в ZCAD по нажатию кнопки.

## 📋 Содержание

- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [Настройка GRIST](#настройка-grist)
- [API Flask сервера](#api-flask-сервера)
- [Тестирование](#тестирование)
- [Устранение неполадок](#устранение-неполадок)

---

## 📁 Расположение файлов

Проект разделён на две части:

- **`server/`** — серверная часть (Flask API, TCP клиент, тесты)
- **`widget/randomline/`** — frontend часть (HTML, CSS, JS для GRIST виджета)

---

## 🏗 Архитектура

```
┌─────────────────────────┐
│  GRIST Widget (Frontend)│
│   - index.html          │
│   - js/app.js           │
│   - css/styles.css      │
└───────────┬─────────────┘
            │ HTTP POST
            │ (JSON)
            ▼
┌─────────────────────────┐
│   Flask API Server      │
│   - flask_server.py     │
│   - zcad_tcp_client.py  │
└───────────┬─────────────┘
            │ TCP socket
            │ (JSON over TCP)
            ▼
┌─────────────────────────┐
│   ZCAD TCP Server       │
│   (порт 7777)           │
└─────────────────────────┘
```

---

## 📁 Структура проекта

```
GristWidgets/
├── server/                     # СЕРВЕРНАЯ ЧАСТЬ
│   ├── flask_server.py         # Flask API сервер
│   ├── zcad_tcp_client.py      # TCP клиент для ZCAD
│   ├── test_zcad_client.py     # Unit-тесты
│   ├── architecture.py         # ASCII схема архитектуры
│   ├── requirements.txt        # Python зависимости
│   ├── start.bat               # Скрипт быстрого запуска
│   ├── README.md               # Эта инструкция
│   └── logs/                   # Папка для логов
│
└── widget/
    └── randomline/             # FRONTEND ЧАСТЬ (GRIST виджет)
        ├── index.html          # HTML виджета для GRIST
        ├── widget.json         # Конфигурация виджета
        ├── grist-plugin-api.js # GRIST API
        ├── README.md           # Документация виджета
        ├── css/
        │   └── styles.css      # Стили виджета
        └── js/
            └── app.js          # Логика виджета (Frontend)
```

---

## 📦 Требования

### Для Flask сервера:
- Python 3.8+
- Flask 3.0+
- flask-cors 4.0+

### Для виджета GRIST:
- GRIST с поддержкой виджетов
- Доступ к Flask серверу из браузера

### Для работы с ZCAD:
- Запущенный ZCAD с активным TCP сервером (порт 7777 по умолчанию)

---

## 🚀 Установка и запуск

### Шаг 1: Установка зависимостей Flask

```bash
cd c:\zcad\GristWidgets\server
pip install -r requirements.txt
```

### Шаг 2: Запуск Flask сервера

**Вариант A: Простой запуск (локально)**

```bash
cd c:\zcad\GristWidgets\server
python flask_server.py
```

Сервер запустится на `http://127.0.0.1:5000`

**Вариант B: Быстрый запуск через start.bat (Windows)**

```bash
cd c:\zcad\GristWidgets\server
start.bat
```

Скрипт автоматически проверит зависимости, запустит Flask сервер и HTTP сервер для виджета.

**Вариант C: С настройкой параметров через переменные окружения**

```bash
# Windows (PowerShell)
$env:FLASK_HOST="0.0.0.0"
$env:FLASK_PORT="5000"
$env:ZCAD_HOST="127.0.0.1"
$env:ZCAD_PORT="7777"
$env:ZCAD_TOKEN=""
$env:FLASK_DEBUG="false"
python flask_server.py
```

```bash
# Linux/Mac
export FLASK_HOST=0.0.0.0
export FLASK_PORT=5000
export ZCAD_HOST=127.0.0.1
export ZCAD_PORT=7777
export FLASK_DEBUG=false
python flask_server.py
```

**Вариант C: Production запуск (gunicorn)**

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 flask_server:app
```

### Шаг 3: Проверка работы Flask

Откройте в браузере:
- `http://127.0.0.1:5000/api/health` - проверка здоровья сервера

---

## ⚙️ Настройка GRIST

### Подключение виджета

1. Откройте GRIST
2. Создайте новую страницу или откройте существующую
3. Добавьте виджет → **Custom Widget**
4. Введите URL виджета:
   - Для локальной разработки: `http://localhost:8080` (нужен локальный HTTP сервер)
   - Для продакшена: URL где размещён виджет

### Быстрый старт локального HTTP сервера для виджета

```bash
# Python HTTP сервер (порт 8080)
cd c:\zcad\GristWidgets\widget\randomline
python -m http.server 8080
```

Или используйте `start.bat` в папке `server/` — он автоматически запустит оба сервера.

### Настройка виджета в GRIST

После добавления виджета настройте опции:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `flaskUrl` | URL Flask API | `http://127.0.0.1:5000` |
| `zcadHost` | Хост ZCAD | `127.0.0.1` |
| `zcadPort` | Порт ZCAD | `7777` |

---

## 📡 API Flask сервера

### POST /api/zcad/draw-random-lines

Генерация случайных линий в ZCAD.

**Request:**
```json
{
  "count": 1000,
  "seed": 42,
  "min_coord": -100,
  "max_coord": 100
}
```

**Response (успех):**
```json
{
  "status": "ok",
  "message": "1000 lines created in ZCAD",
  "details": {
    "requested": 1000,
    "created": 1000,
    "duration_seconds": 1.23,
    "batch_result": "Batch completed"
  },
  "timestamp": "2024-01-01T12:00:00"
}
```

**Response (ошибка):**
```json
{
  "status": "error",
  "message": "Connection refused - is ZCAD running?"
}
```

### POST /api/zcad/ping

Проверка доступности ZCAD.

**Response:**
```json
{
  "status": "ok",
  "message": "ZCAD connected",
  "timestamp": "2024-01-01T12:00:00"
}
```

### POST /api/zcad/line

Создание одной линии.

**Request:**
```json
{
  "x1": 0,
  "y1": 0,
  "x2": 100,
  "y2": 100
}
```

### GET /api/health

Проверка здоровья Flask сервера.

---

## 🧪 Тестирование

### Тест 1: Проверка TCP клиента

```bash
cd c:\zcad\GristWidgets\server
python zcad_tcp_client.py
```

### Тест 2: Проверка Flask + ZCAD

```bash
# Проверка ping
curl -X POST http://127.0.0.1:5000/api/zcad/ping

# Создание 100 линий
curl -X POST http://127.0.0.1:5000/api/zcad/draw-random-lines \
  -H "Content-Type: application/json" \
  -d '{"count": 100}'
```

### Тест 3: Запуск unit-тестов

```bash
cd c:\zcad\GristWidgets\server
python test_zcad_client.py
```

### Тест 4: Интеграционный тест (GRIST → Flask → ZCAD)

1. Убедитесь, что ZCAD запущен
2. Запустите Flask сервер
3. Откройте GRIST с виджетом
4. Нажмите кнопку **"Проверить соединение"**
5. Нажмите кнопку **"Нарисовать 1000 случайных линий"**

---

## 🔧 Устранение неполадок

### Ошибка: "Connection refused"

**Проблема:** ZCAD не запущен или недоступен

**Решение:**
1. Проверьте, что ZCAD запущен
2. Убедитесь, что TCP сервер активен (порт 7777)
3. Проверьте настройки брандмауэра

### Ошибка: "CORS policy"

**Проблема:** Браузер блокирует запросы из GRIST

**Решение:**
1. Убедитесь, что flask-cors установлен
2. Проверьте, что CORS включён в flask_server.py
3. Для production настройте разрешённые_origin

### Ошибка: "Timeout"

**Проблема:** Превышено время ожидания ответа

**Решение:**
1. Увеличьте `requestTimeout` в `widget/randomline/js/app.js`
2. Уменьшите количество линий для тестирования
3. Проверьте производительность ZCAD

### Логи

Логи Flask сервера сохраняются в:
```
server/logs/flask_app.log
```

---

## 📊 Примеры использования

### Создание 5000 линий с фиксированным seed

```javascript
// В консоли браузера или через виджет
fetch('http://127.0.0.1:5000/api/zcad/draw-random-lines', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    count: 5000,
    seed: 12345,
    min_coord: -200,
    max_coord: 200
  })
})
```

### Проверка статуса ZCAD

```bash
curl http://127.0.0.1:5000/api/health
```

---

## 🔐 Безопасность

### Переменные окружения для чувствительных данных

```bash
# .env файл (не коммитить в git!)
ZCAD_TOKEN=your-secret-token
FLASK_SECRET_KEY=your-flask-secret
```

### Настройка CORS для production

```python
CORS(app, resources={
    r"/api/*": {
        "origins": ["https://your-grist-domain.com"],
        "methods": ["POST", "GET"],
        "allow_headers": ["Content-Type"]
    }
})
```

---

## 📝 Лицензия

Виджет распространяется под той же лицензией, что и основной проект ZCAD.

---

## 🤝 Поддержка

Вопросы и предложения направляйте в репозиторий проекта или создайте issue.
