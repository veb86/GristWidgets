/**
 * Модуль инициализации для виджета-органайзера test123
 * Отвечает за дополнительные функции, если они понадобятся
 */

// Инициализация после загрузки DOM
document.addEventListener('DOMContentLoaded', () => {
    console.log('Виджет-органайзер test123 загружен');

    // Здесь можно добавить дополнительную логику при необходимости
    // Например, синхронизация прокрутки между колонками или обмен данными
    
    // Пример: обработка изменения размера окна
    window.addEventListener('resize', () => {
        // При необходимости можно добавить логику адаптации размеров
        console.log('Размер окна изменен');
    });
});

// Обработка ошибок загрузки
window.addEventListener('error', (e) => {
    console.error('Ошибка в виджете test123:', e.error);
});

// Обработка предупреждений
window.addEventListener('unhandledrejection', (e) => {
    console.warn('Необработанное обещание:', e.reason);
});