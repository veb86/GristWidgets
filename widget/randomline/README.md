# Random Line Widget — Frontend (GRIST)

Frontend часть виджета Random Line для GRIST.

## 📁 Структура

```
randomline/
├── index.html              # HTML виджета для GRIST
├── widget.json             # Конфигурация виджета
├── grist-plugin-api.js     # GRIST API
├── css/
│   └── styles.css          # Стили виджета
└── js/
    └── app.js              # Логика виджета (Frontend)
```

## 🚀 Запуск HTTP сервера для разработки

```bash
cd c:\zcad\GristWidgets\widget\randomline
python -m http.server 8080
```

После запуска виджет будет доступен по адресу: `http://localhost:8080`

## ⚙️ Настройка в GRIST

1. Откройте GRIST
2. Добавьте виджет → **Custom Widget**
3. Введите URL виджета: `http://localhost:8080` (для локальной разработки)

### Опции виджета

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `flaskUrl` | URL Flask API | `http://127.0.0.1:5000` |
| `zcadHost` | Хост ZCAD | `127.0.0.1` |
| `zcadPort` | Порт ZCAD | `7777` |

## 🔗 Серверная часть

Серверная часть (Flask API) находится в папке `server/` в корне проекта.

Для запуска сервера:

```bash
cd c:\zcad\GristWidgets\server
python flask_server.py
```

Или используйте скрипт быстрого запуска:

```bash
cd c:\zcad\GristWidgets\server
start.bat
```

См. полную документацию в `server/README.md`.

## 📋 Архитектура

```
GRIST Widget (Frontend)
       │
       │ HTTP POST (JSON)
       ▼
Flask API Server (server/)
       │
       │ TCP Socket (JSON over TCP)
       ▼
ZCAD TCP Server (порт 7777)
```
