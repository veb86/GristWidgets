# Multipage Widget для Grist

Многостраничный кастомный виджет для Grist, реализующий архитектуру SPA (Single Page Application) с возможностью переключения между независимыми экранами.

## 📋 Описание

Виджет представляет собой мини-приложение внутри Grist с тремя страницами:

| Страница | Описание |
|----------|----------|
| **Главная** | Приветственная страница с общей информацией |
| **Детали** | Детальное отображение текущей записи Grist |
| **Настройки** | Управление настройками виджета |

## 🚀 Возможности

- ✅ **Переключение страниц по кнопкам** — навигация через меню и кнопки в интерфейсе
- ✅ **Программное переключение** — автоматический переход по событиям из Grist (например, при изменении поля `Status`)
- ✅ **Сохранение состояния** — активная страница сохраняется в URL hash и localStorage
- ✅ **Анимации переходов** — плавные fade/slide анимации при переключении
- ✅ **Интеграция с Grist API** — использование `onRecord()`, `onRecords()`, `onOptions()`
- ✅ **Режим разработки** — работа без подключения к Grist с эмуляцией данных

## 📁 Структура проекта

```
multipage/
├── index.html          # Главный HTML-файл с разметкой
├── app.js              # Главный модуль приложения и навигационный контроллер
├── styles.css          # Стили и анимации
├── widget.json         # Конфигурация виджета для Grist
├── README.md           # Документация
└── pages/
    ├── home.js         # Модуль страницы "Главная"
    ├── details.js      # Модуль страницы "Детали"
    └── settings.js     # Модуль страницы "Настройки"
```

## 🏗️ Архитектура

### Модульная структура

Каждый модуль реализует стандартный интерфейс:

```javascript
var ModuleName = (function() {
    'use strict';

    // Приватные переменные
    var privateVar = null;

    // Публичные методы
    function init() { }
    function onRecord(record) { }
    function onRecords(records) { }
    function onNavigate() { }

    return {
        init: init,
        onRecord: onRecord,
        onRecords: onRecords,
        onNavigate: onNavigate
    };
})();
```

### Навигационный контроллер (AppModule)

Центральный модуль управляет:
- Переключением между страницами
- Интеграцией с Grist API
- Сохранением и восстановлением состояния
- Обработкой событий навигации

### API модулей

| Метод | Описание |
|-------|----------|
| `init()` | Инициализация модуля |
| `onRecord(record)` | Вызывается при получении записи из Grist |
| `onRecords(records)` | Вызывается при получении списка записей |
| `onNavigate()` | Вызывается при переходе на эту страницу |

## 🔧 Установка

1. Скопируйте папку `multipage` в директорию виджетов Grist
2. В интерфейсе Grist добавьте новый виджет
3. Выберите `Multipage Widget` из списка доступных
4. Настройте привязку к таблице

## 📖 Использование

### Базовая навигация

```javascript
// Переход к странице "Детали"
AppModule.navigateTo('details');

// Переход к странице "Главная"
AppModule.navigateTo('home');

// Переход к странице "Настройки"
AppModule.navigateTo('settings');
```

### Автоматическое переключение по статусу

В настройках виджета включите опцию **"Автоматическое переключение по статусу"**:

- Если `Status = "Done"` → открывается страница «Детали»
- Если `Status = "Draft"` → открывается страница «Главная»

### Работа с данными Grist

```javascript
// Получение текущей записи
grist.onRecord(function(record) {
    console.log('Текущая запись:', record);
    // record.id, record.Name, record.Status, ...
});

// Получение списка записей
grist.onRecords(function(records) {
    console.log('Записей:', records.length);
});

// Обновление поля записи
grist.docApi.applyUserActions([
    ['UpdateRecord', recordId, {FieldName: newValue}]
]);
```

## ⚙️ Настройки

### Страница по умолчанию

Выберите страницу, которая будет открываться при загрузке виджета:
- Главная
- Детали
- Настройки

### Автоматическое переключение

При включении этой опции виджет автоматически переключается между страницами в зависимости от значения поля `Status` в текущей записи.

## 🎨 Стилизация

Виджет использует **Bootstrap 5** для базовых стилей и кастомные CSS-анимации для переходов между страницами.

### Доступные анимации

| Класс | Описание |
|-------|----------|
| `page-enter` | Базовая fade-анимация |
| `page-enter-from-right` | Анимация slide справа |
| `page-enter-from-left` | Анимация slide слева |
| `page-exit` | Анимация исчезновения |

### Тёмная тема

Виджет автоматически поддерживает системную тёмную тему через `prefers-color-scheme: dark`.

## 🔌 Расширение функциональности

### Добавление новой страницы

1. Создайте файл `pages/newpage.js`:

```javascript
var NewPageModule = (function() {
    'use strict';

    var currentRecord = null;

    function init() {
        console.log('[NewPageModule] Инициализация');
    }

    function onRecord(record) {
        currentRecord = record;
    }

    function onNavigate() {
        console.log('[NewPageModule] Переход на страницу');
    }

    return {
        init: init,
        onRecord: onRecord,
        onNavigate: onNavigate
    };
})();
```

2. Добавьте секцию в `index.html`:

```html
<section id="page-newpage" class="page-content" style="display: none;">
    <!-- Содержимое страницы -->
</section>
```

3. Добавьте ссылку в навигацию:

```html
<li class="nav-item">
    <a class="nav-link" href="#view=newpage" data-page="newpage">Новая</a>
</li>
```

4. Зарегистрируйте модуль в `app.js`:

```javascript
var AppModule = (function(HomeModule, DetailsModule, SettingsModule, NewPageModule) {
    // ...
    PAGES = {
        HOME: 'home',
        DETAILS: 'details',
        SETTINGS: 'settings',
        NEWPAGE: 'newpage'  // Добавить новую страницу
    };
    // ...
})(HomeModule, DetailsModule, SettingsModule, NewPageModule);
```

## 🐛 Отладка

### Режим разработки

При запуске вне Grist виджет автоматически переходит в режим разработки:
- Эмулируется подключение к Grist API
- Через 1 секунду загружается тестовая запись
- Все функции работают в ограниченном режиме

### Логирование

Виджет выводит подробные логи в консоль браузера:
- `[AppModule]` — события главного модуля
- `[HomeModule]` — события страницы "Главная"
- `[DetailsModule]` — события страницы "Детали"
- `[SettingsModule]` — события страницы "Настройки"

## 📝 Требования

- Grist Core или Grist Cloud
- Современный браузер (Chrome, Firefox, Edge, Safari)
- Поддержка JavaScript ES5+

## 📄 Лицензия

MIT License

## 🔗 Ссылки

- [Grist Labs](https://www.getgrist.com/)
- [Grist Core (GitHub)](https://github.com/gristlabs/grist-core)
- [Grist Widget (GitHub)](https://github.com/gristlabs/grist-widget)
