# Отладка BDControl Widget

## Проблема: Виджет не работает, ничего не происходит

### Шаг 1: Откройте консоль браузера

1. Откройте виджет в Grist
2. Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
3. Перейдите на вкладку **Console**

### Шаг 2: Проверьте логи

Вы должны увидеть следующие сообщения:

```
Инициализация виджета BDControl...
Grist API инициализирован
Загрузка таблиц через docApi.fetchTable...
[GristApi] Загрузка таблицы: Device_groups
Device_groups: X записей
[GristApi] Загрузка таблицы: Device
Device: X записей
[GristApi] Загрузка таблицы: SYSTEM
SYSTEM: X записей
...
Все данные загружены, отрисовка...
```

### Шаг 3: Используйте страницу отладки

Откройте файл `debug.html` в контексте вашего Grist документа:

1. В Grist откройте любую страницу
2. Добавьте виджет на страницу
3. В настройках виджета укажите путь к `debug.html`
4. Или откройте через браузер: `http://localhost:8080/widget/BDControl/debug.html`

Страница отладки покажет:
- ✅ Какие таблицы загрузились
- ✅ Сколько записей в каждой таблице
- ✅ Данные таблиц в виде таблиц
- ✅ Тест характеристик для групп 4 и 5

### Шаг 4: Проверьте данные в таблицах

#### Device_groups
Убедитесь что есть группы с `id=4` (Светильники) и `id=5` (Лампы):

```
id | parent_id | code       | name
4  | 1         | luminaires | Светильники
5  | 1         | lamps      | Лампы
```

#### Characteristics
Убедитесь что есть характеристики:

```
id | code          | name           | data_type | unit
1  | power         | Мощность       | float     | Вт
2  | voltage       | Напряжение питания | string  | В
3  | luminous_flux | Световой поток | integer   | лм
4  | color_temperature | Температура | integer  | К
```

#### Group_Characteristics
Убедитесь что есть связи для групп 4 и 5:

```
id | group_id | characteristic_id | is_visible | sort_order
1  | 4        | 1                 | true       | 1
2  | 4        | 2                 | true       | 2
3  | 4        | 3                 | true       | 3
4  | 4        | 4                 | true       | 4
5  | 5        | 1                 | true       | 1
6  | 5        | 3                 | true       | 2
7  | 5        | 4                 | true       | 3
```

#### Device_characteristics
Убедитесь что есть значения характеристик для устройств:

```
id | device_id | characteristic_id | value_float | value_int | value_string
1  | 1         | 1                 | 40          |           |
2  | 1         | 2                 |             |           | 220
3  | 1         | 3                 |             | 4200      |
```

#### SYSTEM
Убедитесь что есть параметр `selectedGroupID`:

```
id | param           | value
1  | selectedGroupID | 4
```

### Шаг 5: Проверьте имена таблиц в Grist

Имена таблиц должны **точно совпадать** (чувствительно к регистру):

| Ожидаемое имя | Проверьте в Grist |
|---------------|-------------------|
| `Device_groups` | Device_groups ✅ |
| `Device` | Device ✅ |
| `SYSTEM` | SYSTEM ✅ |
| `Group_Characteristics` | Group_Characteristics ✅ |
| `Characteristics` | Characteristics ✅ |
| `Device_characteristics` | Device_characteristics ✅ |

### Шаг 6: Типичные ошибки

#### Ошибка: "Table not found"

```
[GristApi] Ошибка загрузки таблицы Group_Characteristics: Table not found
```

**Решение:** Убедитесь что таблица существует в Grist и имя написано правильно.

#### Ошибка: "Access denied"

```
Access denied to table SYSTEM
```

**Решение:** Проверьте права доступа к таблице. Виджет требует `full` доступ.

#### Ошибка: "Cannot read property 'filter' of undefined"

```
TypeError: Cannot read property 'filter' of undefined
```

**Решение:** Таблица загрузилась пустой. Проверьте что в таблице есть данные.

#### Виджет отображает только базовые столбцы

**Проблема:** Характеристики не загружены или не настроены.

**Решение:**
1. Проверьте что `Group_Characteristics` содержит записи для вашей группы
2. Проверьте что `characteristic_id` ссылается на существующие характеристики
3. Проверьте что `is_visible = true`

### Шаг 7: Тест в debug.html

1. Откройте `debug.html`
2. Нажмите кнопку **"🔄 Загрузить данные"**
3. Проверьте что все таблицы показали количество записей
4. Нажмите кнопку **"🧪 Тест характеристик"**
5. Проверьте логи:

```
=== Тест характеристик ===
Группа 4 (Светильники):
  - Мощность (power), unit: Вт, visible: true
  - Напряжение питания (voltage), unit: В, visible: true
  - Световой поток (luminous_flux), unit: лм, visible: true
  - Температура (color_temperature), unit: К, visible: true
Группа 5 (Лампы):
  - Мощность (power), unit: Вт, visible: true
  - Световой поток (luminous_flux), unit: лм, visible: true
  - Температура (color_temperature), unit: К, visible: true
=== Тест завершён ===
```

### Шаг 8: Проверьте виджет в Grist

1. Добавьте виджет на страницу в Grist
2. В таблице `SYSTEM` установите `selectedGroupID = 4` (Светильники)
3. Виджет должен отобразить таблицу со столбцами:
   - Наименование, Модель, Производитель, Ед. изм., Количество, Примечание, Группа
   - Мощность (Вт), Напряжение питания (В), Световой поток (лм), Температура (К)

4. Измените `selectedGroupID = 5` (Лампы)
5. Виджет должен отобразить таблицу со столбцами:
   - Наименование, Модель, Производитель, Ед. изм., Количество, Примечание, Группа
   - Мощность (Вт), Световой поток (лм), Температура (К)

**Обратите внимание:** Для ламп не должно быть столбца "Напряжение питания".

## Быстрая проверка

Выполните этот код в консоли браузера:

```javascript
// Проверка данных
grist.docApi.fetchTable('Device_groups').then(d => console.log('Device_groups:', d.id.length));
grist.docApi.fetchTable('Device').then(d => console.log('Device:', d.id.length));
grist.docApi.fetchTable('SYSTEM').then(d => console.log('SYSTEM:', d.id.length));
grist.docApi.fetchTable('Group_Characteristics').then(d => console.log('Group_Characteristics:', d.id.length));
grist.docApi.fetchTable('Characteristics').then(d => console.log('Characteristics:', d.id.length));
grist.docApi.fetchTable('Device_characteristics').then(d => console.log('Device_characteristics:', d.id.length));
```

Все таблицы должны показать количество записей > 0.

## Контакты

Если проблема не решена, проверьте:
1. Версию Tabulator (должна быть 5.5.0)
2. Что виджет загружается в iframe в Grist
3. Что нет ошибок CORS
