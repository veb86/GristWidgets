# 🔧 Настройка таблиц в Grist для виджета BDControl

## ⚠️ Важно

Виджет BDControl требует наличия **трёх таблиц** в документе Grist:

1. `Device_groups`
2. `Device`
3. `SYSTEM`

**Обратите внимание:** Имена таблиц **чувствительны к регистру**!

---

## 📋 Шаг 1: Создание таблицы Device_groups

### 1.1 Создайте новую таблицу

1. В Grist нажмите **+** (Add New) → **Add New Table**
2. Назовите таблицу: `Device_groups` (с заглавной буквы)

### 1.2 Добавьте столбцы

| Столбец    | Тип данных | Описание                          | Пример |
| ---------- | ---------- | --------------------------------- | ------ |
| id         | Int        | Уникальный ID группы (ключ)       | 1      |
| manualSort | Int        | Порядок сортировки                | 1      |
| parent_id  | Int        | Родительская группа (0 = корневая)| 0      |
| code       | Text       | Технический код группы            | lighting |
| name       | Text       | Отображаемое имя группы           | Освещение |

### 1.3 Пример данных

| id | manualSort | parent_id | code       | name                       |
|----|------------|-----------|------------|----------------------------|
| 1  | 1          | 0         | lighting   | Освещение                  |
| 2  | 2          | 0         | protection | Защита                     |
| 3  | 3          | 0         | cables     | Кабели                     |
| 4  | 1          | 1         | luminaires | Светильники                |
| 5  | 2          | 1         | lamps      | Лампы                      |
| 6  | 1          | 2         | breakers   | Автоматические выключатели |

**Иерархия:**
```
Освещение (1)
 ├─ Светильники (4)
 └─ Лампы (5)

Защита (2)
 └─ Автоматические выключатели (6)

Кабели (3)
```

---

## 📋 Шаг 2: Создание таблицы Device

### 2.1 Создайте новую таблицу

1. В Grist нажмите **+** (Add New) → **Add New Table**
2. Назовите таблицу: `Device` (с заглавной буквы)

### 2.2 Добавьте столбцы

| Столбец              | Тип данных | Описание                     | Пример              |
| -------------------- | ---------- | ---------------------------- | ------------------- |
| id                   | Int        | ID устройства (ключ)         | 1                   |
| name                 | Text       | Наименование                 | Светильник LED-001  |
| brand                | Text       | Модель                       | Philips             |
| factoryname          | Text       | Производитель                | Philips Lighting    |
| devgroup_id          | Int        | ID группы устройства         | 4                   |
| gristHelper_Display2 | Text       | Отображаемое имя группы      | Светильники         |
| unit                 | Text       | Единица измерения            | шт                  |
| count                | Text       | Количество                   | 10                  |
| note                 | Text       | Примечание                   | Для офиса           |

### 2.3 Пример данных

| id | name                 | brand    | factoryname        | devgroup_id | gristHelper_Display2 | unit | count | note      |
|----|----------------------|----------|--------------------|-------------|----------------------|------|-------|-----------|
| 1  | Светильник LED-001   | Philips  | Philips Lighting   | 4           | Светильники          | шт   | 10    | Для офиса |
| 2  | Светильник LED-002   | Osram    | Osram GmbH         | 4           | Светильники          | шт   | 5     | Для склада|
| 3  | Лампа накаливания 60W| General Electric | GE Lighting | 5           | Лампы                | шт   | 50    |           |
| 4  | Лампа светодиодная E27| Gauss   | Gauss LLC          | 5           | Лампы                | шт   | 100   | Тёплый свет|
| 5  | Автомат 1P C16       | ABB      | ABB Stotz-Kontakt  | 6           | Автоматические выключатели | шт | 20 |        |
| 6  | Кабель ВВГнг 3x2.5   | Камкабель| Камский кабель     | 3           | Кабели               | м    | 500   | Бухта 100м|

---

## 📋 Шаг 3: Создание таблицы SYSTEM

### 3.1 Создайте новую таблицу

1. В Grist нажмите **+** (Add New) → **Add New Table**
2. Назовите таблицу: `SYSTEM` (заглавными буквами)

### 3.2 Добавьте столбцы

| Столбец | Тип данных | Описание           | Пример          |
| ------- | ---------- | ------------------ | --------------- |
| id      | Int        | ID записи (ключ)   | 1               |
| param   | Text       | Название параметра | selectedGroupID |
| value   | Text       | Значение параметра | 1               |

### 3.3 Пример данных

| id | param           | value |
|----|-----------------|-------|
| 1  | selectedGroupID | 1     |

**Важно:** Значение `value` должно быть **ID существующей группы** из таблицы `Device_groups`.

---

## ✅ Проверка настройки

### Проверка 1: Таблицы созданы

Убедитесь, что в документе Grist есть три таблицы с точными именами:
- [ ] `Device_groups` (с заглавной D)
- [ ] `Device` (с заглавной D)
- [ ] `SYSTEM` (заглавными буквами)

### Проверка 2: Данные заполнены

- [ ] В `Device_groups` есть хотя бы одна группа
- [ ] В `Device` есть устройства с `devgroup_id`, ссылающимся на группы
- [ ] В `SYSTEM` есть запись с `param = 'selectedGroupID'`

### Проверка 3: Ссылки корректны

- [ ] `Device.devgroup_id` ссылается на существующий `Device_groups.id`
- [ ] `SYSTEM.value` содержит существующий ID группы

---

## 🚀 Установка виджета

После создания таблиц:

1. Добавьте Custom Widget на страницу
2. URL виджета: путь к `index.html`
3. Виджет автоматически загрузит данные через `grist.docApi.fetchTable()`

**Примечание:** Виджет требует доступ `full` для чтения таблиц через docApi.

---

## 🔍 Troubleshooting

### Ошибка: KeyError 'Device_groups'

**Причина:** Таблица не существует или называется иначе

**Решение:**
1. Проверьте имя таблицы в Grist
2. Убедитесь, что таблица называется точно `Device_groups`

### Ошибка: KeyError 'Device'

**Причина:** Таблица не существует или называется иначе

**Решение:**
1. Проверьте имя таблицы в Grist
2. Убедитесь, что таблица называется точно `Device`

### Ошибка: KeyError 'SYSTEM'

**Причина:** Таблица не существует или называется иначе

**Решение:**
1. Проверьте имя таблицы в Grist
2. Убедитесь, что таблица называется точно `SYSTEM`

### Виджет показывает «Группа не выбрана»

**Причина:** В таблице `SYSTEM` нет записи с `param = 'selectedGroupID'`

**Решение:**
1. Добавьте запись в `SYSTEM`:
   - `param` = `selectedGroupID`
   - `value` = ID группы (например, `1`)

### Виджет показывает «В данной группе нет устройств»

**Причина:** В выбранной группе нет устройств

**Решение:**
1. Проверьте, что в таблице `Device` есть устройства с `devgroup_id` равным ID выбранной группы или её дочерних групп

---

## 📝 Примечания

- Имена таблиц **чувствительны к регистру**
- `Device_groups` и `Device` - с заглавной буквы
- `SYSTEM` - заглавными буквами
- Убедитесь, что в именах нет лишних пробелов
- Для работы иерархии правильно установите `parent_id` в `Device_groups`
